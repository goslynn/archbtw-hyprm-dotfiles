# Program aliases — referenced by keybinds and autostart
$terminal    = kitty
$menu        = fuzzel
$browser     = brave
# Default file manager: yazi (registered as inode/directory handler via mimeapps.list)
$fileManager = kitty -e yazi
